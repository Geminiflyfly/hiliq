{
  "name": "hiliq",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages + R2 lightweight image host",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler pages dev public --r2=BUCKET=hiliq-images --ip 0.0.0.0 --port 8788",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
